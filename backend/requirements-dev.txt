# AP2WEB — development / CI-only test tooling.
# NOT part of the production runtime image (Dockerfile installs requirements.txt
# only). CI installs this file to run the quality gates.
pytest==9.1.1
pytest-timeout==2.4.0
ruff==0.11.2
mypy==1.14.1
